import Resend from '../components/Resend.tsx';

const ResendPage = () =>
{

    return(
      <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh', background: 'url(../src/assets/pattern.png)'}}>
        <Resend/>
      </div>

    );
};

export default ResendPage;
